# Browser uses the same-origin proxy, so no CORS configuration is needed.
NEXT_PUBLIC_API_BASE_URL=/backend
# Next.js forwards /backend requests to this FastAPI origin.
BACKEND_API_URL=http://127.0.0.1:8000
